import { TableFilterMenorPipe } from './table-filter-menor.pipe';

describe('TableFilterMenorPipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilterMenorPipe();
    expect(pipe).toBeTruthy();
  });
});
