import { TableFilteredPipe } from './table-filtered.pipe';

describe('TableFilteredPipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilteredPipe();
    expect(pipe).toBeTruthy();
  });
});
