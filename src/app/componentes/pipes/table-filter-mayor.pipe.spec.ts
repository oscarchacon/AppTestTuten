import { TableFilterMayorPipe } from './table-filter-mayor.pipe';

describe('TableFilterMayorPipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilterMayorPipe();
    expect(pipe).toBeTruthy();
  });
});
